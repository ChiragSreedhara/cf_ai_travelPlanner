The following prompts were used on Google Gemini 2.5 Pro to help build the structure and debug the code for this project.

* "How do I create a Cloudflare Worker project that includes Durable Objects?"
* "What is the `wrangler.json` syntax for binding Workers AI and a Durable Object?"
* "My frontend on `pages.dev` is getting a CORS error when fetching my `workers.dev` backend. How do I fix this?"
* "Write the JavaScript code to handle a CORS preflight OPTIONS request in a Cloudflare Worker."
* "My AI response is getting cut off mid-sentence, what's happening?"
* "My worker is crashing with a 5021 token limit error. What does this mean and how do I fix it?"
* "How do I clear the memory of a Durable Object to start a new conversation?"
* "My app is responding with an empty line. How do I debug this?"